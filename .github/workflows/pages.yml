name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inject Gemini API Key
        env:
          GEMINI_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python3 -c "import os; k=os.environ['GEMINI_KEY']; [open(f,'w').write(open(f).read().replace('%%GEMINI_API_KEY%%',k)) for f in ['index.html','nahrungsergaenzungsmittel-experte.html']]; print('Done')"
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - id: deployment
        uses: actions/deploy-pages@v4
